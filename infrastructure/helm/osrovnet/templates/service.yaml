apiVersion: v1
kind: Service
metadata:
  name: {{ include "osrovnet.fullname" . }}-backend
  labels:
    {{- include "osrovnet.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osrovnet.backend.selectorLabels" . | nindent 4 }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "osrovnet.fullname" . }}-frontend
  labels:
    {{- include "osrovnet.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osrovnet.frontend.selectorLabels" . | nindent 4 }}