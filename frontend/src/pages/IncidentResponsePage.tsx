import React from 'react';
import { Box, Typography } from '@mui/material';

export default function IncidentResponsePage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Incident Response</Typography>
      <Typography>Alert triage, playbooks, SOAR integration and case management.</Typography>
    </Box>
  );
}
