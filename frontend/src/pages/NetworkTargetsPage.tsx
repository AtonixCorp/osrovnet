import React from 'react';
import { Box, Typography } from '@mui/material';

export default function NetworkTargetsPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Network Targets</Typography>
      <Typography>Manage monitored assets, groups, profiles and trust zones.</Typography>
    </Box>
  );
}
