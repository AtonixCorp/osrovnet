import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ScanEnginePage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Scan Engine</Typography>
      <Typography>Launch scans, view history and configure parameters.</Typography>
    </Box>
  );
}
