import React from 'react';
import { Box, Typography } from '@mui/material';

export default function TelemetryLogsPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Telemetry & Logs</Typography>
      <Typography>Real-time traffic, packet inspection and historical logs.</Typography>
    </Box>
  );
}
