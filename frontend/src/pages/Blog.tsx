import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Blog() {
  return (
    <Box>
      <Typography variant="h4">Security Blog</Typography>
      <Typography variant="body1">Latest security posts and analyses.</Typography>
    </Box>
  );
}
